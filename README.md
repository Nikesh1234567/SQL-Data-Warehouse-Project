# SQL-Data-Warehouse-Project
This project contains three layers, i.e. Bronze, Silver and Gold. In bronze layer, the raw data is inserted from source to SQL Server without any transformation. In silver Layer, data is inserted from bronze to silver after doing data transformation. In gold layer, data is divided into dimension and fact tables.
